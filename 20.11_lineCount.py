# Python program to get Line Count of a File
# In this example, you will learn to get line count of a file.
'''def file_len(fname):
    with open(fname) as f:
        for i, j in enumerate(f):
           pass
    return(i+1)
print (file_len('Oracle Login ID.csv'))

# Using the list comprehension:
Count = sum(1 for i in open('Oracle Login ID.csv'))
print(Count)
Count_2 = sum(1 for i in open('Questions DBMS.rtf'))
print(Count_2)''' # 77234